// MODALS
	// add ticker to watchlist modal
	$('#addTickerModal').on('shown.bs.modal', function () {
	  $('#myInput').focus();
	});

	// nightly market data update modal
	$('#nightlyUpdateModal').on('shown.bs.modal', function () {
	  $('#myInput').focus();
	});
